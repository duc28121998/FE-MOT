import styled from 'styled-components'
import { Slider } from 'antd'

export const DivSlider = styled(Slider)`
  .ant-slider {
    &-step {
      background-color: black;
      color: black;
      background: transparent;
    }
  }
`
